# ts-jest@29 still declares a peer range of typescript "<7"; the project runs
# typescript 7 and ts-jest works with it in practice. Allow the install to
# proceed (also required for `npm install` on Vercel).
legacy-peer-deps=true
